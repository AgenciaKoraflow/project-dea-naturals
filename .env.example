# Configurações do Servidor
PORT=3000
NODE_ENV=development

# ============================================
# Credenciais da API do Mercado Livre
# ============================================
# IMPORTANTE: Nunca commite este arquivo com valores reais!
# Este arquivo serve apenas como template.
# Copie este arquivo para .env e preencha com suas credenciais reais.

# Client ID da sua aplicação no Mercado Livre
# Obtenha em: https://developers.mercadolivre.com.br/
ML_CLIENT_ID=seu_client_id_aqui

# Client Secret da sua aplicação no Mercado Livre
# Mantenha esta informação em SEGREDO ABSOLUTO
ML_CLIENT_SECRET=seu_client_secret_aqui

# URI de redirecionamento configurada na sua aplicação
# Deve ser exatamente igual à configurada no painel do desenvolvedor
ML_REDIRECT_URI=http://localhost:3000/callback

# Código de autorização (obtido após o usuário autorizar sua aplicação)
# Este código é temporário e deve ser usado apenas uma vez
# Para obter o código, redirecione o usuário para:
# https://auth.mercadolivre.com.br/authorization?response_type=code&client_id=SEU_CLIENT_ID&redirect_uri=SUA_REDIRECT_URI
ML_AUTHORIZATION_CODE=seu_codigo_de_autorizacao_aqui

# Refresh Token (obtido após usar o authorization code pela primeira vez)
# Use este token para renovar o access token quando ele expirar
ML_REFRESH_TOKEN=seu_refresh_token_aqui
